{%extends 'products/itemBase.html.twig'%}
{% block detail %}
  <h2> Detalle de Productos </h2>
    <div class="container">
        <div class="row">
          <div class="col text-center">
            <h2>{{ item.description }} - {{ item.scale }}</h2>
          </div>
        </div>
        <div class="row">
          <div class="col"><img src="./assets/image/{{ item.image }}" alt="item image"/></div>
          <div class="col">
            <div class="row"><div class="col">{{ item.brand }}</div></div>
            <div class="row">
              <ul>
                {# DEBILIDADES #}
              </ul>
            </div>
          </div>
        </div>
      </div>
{% endblock %}
